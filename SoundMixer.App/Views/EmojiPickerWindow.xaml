<Window x:Class="SoundMixer.App.Views.EmojiPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SoundMixer.App.Views"
        Title="Выберите эмодзи"
        Width="420" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF2A2A3A">

    <Window.Resources>
        <local:IntToVisibilityConverter x:Key="IntToVisConverter" />
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0"
                     FontSize="14" Padding="8,6"
                     Background="#FF3D3D54" Foreground="White"
                     BorderBrush="#FF555577"
                     TextChanged="SearchBox_TextChanged" />
            <TextBlock Grid.Column="0" Text="Поиск..."
                       Foreground="#66FFFFFF" FontSize="14"
                       Margin="12,7,0,0" IsHitTestVisible="False"
                       Visibility="{Binding ElementName=SearchBox, Path=Text.Length,
                                    Converter={StaticResource IntToVisConverter}}" />
            <TextBlock Grid.Column="1" x:Name="SelectedPreview"
                       FontSize="32" Margin="8,0,0,0" VerticalAlignment="Center" />
        </Grid>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled" Margin="0,0,0,6">
            <StackPanel x:Name="CategoryPanel" Orientation="Horizontal" />
        </ScrollViewer>

        <!-- Светлый фон для эмодзи -->
        <Border Grid.Row="2" Background="#FFFAFAFA" CornerRadius="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="EmojiGrid">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Отмена" Click="Cancel_Click"
                    Background="#FF3D3D54" Foreground="White"
                    Padding="16,8" Margin="4,0" FontSize="13"
                    BorderThickness="0" Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>